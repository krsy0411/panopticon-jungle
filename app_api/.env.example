# App API 환경변수 템플릿
# 로컬 개발: .env.development 파일 생성 후 값 입력
# 배포: ECS 환경변수로 직접 설정

# PostgreSQL 연결설정
DB_HOST=<your-db-host>          # 예: localhost (로컬) 또는 RDS 엔드포인트 (배포)
DB_PORT=5432
DB_USER=<your-db-user>
DB_PASSWORD=<your-db-password>
DB_NAME=panopticon
DB_SSL=false                    # 배포 환경에서는 true
